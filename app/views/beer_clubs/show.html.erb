<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @beer_club.name %>
</p>

<p>
  <strong>Founded:</strong>
  <%= @beer_club.founded %>
</p>

<p>
  <strong>City:</strong>
  <%= @beer_club.city %>
</p>
<strong>Club members:</strong>
<ul>
  <% @beer_club.memberships.each do |m| %>
    <li> <%= m.user %> <%= link_to ' delete memerships', m, :method => :delete, data: { confirm: 'Are you sure?' } %></li>
  <% end %>
</ul>
<%= link_to 'Edit', edit_beer_club_path(@beer_club) %> |
<%= link_to 'Back', beer_clubs_path %>
